<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_fund_bill_list" model="ir.ui.view">
        <field name="name">nn.fund.bill.list</field>
        <field name="model">nn.fund.bill</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="date"/>
                <field name="requisition_id"/>
                <field name="vendor"/>
                <field name="reference"/>
                <field name="amount"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <record id="view_fund_bill_form" model="ir.ui.view">
        <field name="name">nn.fund.bill.form</field>
        <field name="model">nn.fund.bill</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_post" type="object" string="Post" class="btn-primary" invisible="status != 'draft'"/>
                    <button name="action_reverse" type="object" string="Reverse" invisible="status != 'posted'"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="status != 'draft'"/>
                    <field name="status" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="requisition_id"/>
                            <field name="amount"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="vendor"/>
                            <field name="reference"/>
                            <field name="attachment" filename="attachment_filename"/>
                            <field name="attachment_filename" invisible="1"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_fund_bill" model="ir.actions.act_window">
        <field name="name">Bills</field>
        <field name="res_model">nn.fund.bill</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
